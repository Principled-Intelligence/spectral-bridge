# responses-passthrough adapter

Transparent proxy for targets that already expose an OpenAI-compatible `POST /v1/responses` endpoint.

For full documentation see [spectral.principled.app/docs](https://spectral.principled.app/docs/spectral-bridge/adapters/responses-passthrough).
